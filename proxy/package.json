{
  "name": "proxy",
  "version": "1.0.0",
  "description": "*npm install express* // serves as a proxy/back server for t heclient server(local server) and makes https ap requests on its behalf",
  "main": "proxy.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/proxy.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
